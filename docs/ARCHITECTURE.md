# 架构设计文档

## 版本历史

| 版本 | 日期 | 说明 |
|------|------|------|
| v1.0 | 待定 | MVP版本：简化架构，聚焦核心功能 |
| v2.0 | 待定 | 完善版本：引入缓存、分层结构 |
| v3.0 | 待定 | 扩展版本：微服务化、容器化部署 |

---

## 一、技术栈（v1.0）

| 层级 | 技术 | 说明 |
|------|------|------|
| 后端 | Spring Boot 3.x | 应用框架 |
| 数据库 | MySQL 8.0 | 关系数据库 |
| 前端 | Vue 3 | 前端框架 |
| 构建 | Maven | 项目构建 |

---

## 二、系统架构（v1.0 简化版）

```
┌─────────────────────────────────────────────────┐
│                    前端 (Vue 3)                 │
└─────────────────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────┐
│               后端 (Spring Boot)                │
│  ┌───────────┐  ┌───────────┐  ┌───────────┐ │
│  │  Controller │  │  Service  │  │   Mapper  │ │
│  └───────────┘  └───────────┘  └───────────┘ │
└─────────────────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────┐
│                   MySQL 8.0                     │
└─────────────────────────────────────────────────┘
```

---

## 三、模块划分（v1.0）

```
family-genealogy/
├── src/main/java/com/family/genealogy/
│   ├── controller/     # 控制器层
│   ├── service/        # 业务逻辑层
│   ├── mapper/         # 数据访问层
│   ├── entity/         # 实体类
│   ├── dto/            # 数据传输对象
│   └── config/         # 配置类
├── src/main/resources/
│   └── mapper/         # MyBatis映射文件
└── src/test/           # 测试代码
```

---

## 四、数据库架构（v1.0）

### 4.1 数据库选型

| 数据库 | 用途 |
|--------|------|
| MySQL 8.0 | 主数据存储 |

### 4.2 核心表结构

**用户表 (t_user)**

| 字段 | 类型 | 说明 |
|------|------|------|
| id | BIGINT | 主键 |
| phone | VARCHAR(20) | 手机号 |
| password | VARCHAR(255) | 密码（加密） |
| name | VARCHAR(50) | 姓名 |
| role | ENUM | 角色：ADMIN/MEMBER |
| created_at | DATETIME | 创建时间 |

**成员表 (t_member)**

| 字段 | 类型 | 说明 |
|------|------|------|
| id | BIGINT | 主键 |
| family_id | BIGINT | 家族ID |
| name | VARCHAR(50) | 姓名 |
| gender | ENUM | 性别：MALE/FEMALE |
| birth_date | DATE | 出生日期 |
| father_id | BIGINT | 父亲ID |
| mother_id | BIGINT | 母亲ID |
| generation | INT | 辈分 |
| status | ENUM | 状态：ALIVE/DECEASED |
| created_at | DATETIME | 创建时间 |

---

## 五、安全设计（v1.0）

| 要求 | 实现方式 |
|------|----------|
| 密码存储 | BCrypt加密 |
| 接口认证 | JWT Token |
| 传输安全 | HTTPS（生产环境） |

---

## 六、部署架构（v1.0 简化版）

### 6.1 开发环境

```
前端：Vue 3 (开发服务器)
  ↓
后端：Spring Boot (本地运行)
  ↓
数据库：MySQL (本地Docker)
```

### 6.2 生产环境（v3.0规划）

```
                    ┌─────────────┐
                    │   Nginx     │
                    └──────┬──────┘
                           │
          ┌────────────────┼────────────────┐
          ▼                ▼                ▼
    ┌───────────┐   ┌───────────┐   ┌───────────┐
    │  应用实例1  │   │  应用实例2  │   │  应用实例3  │
    └───────────┘   └───────────┘   └───────────┘
                           │
          ┌────────────────┼────────────────┐
          ▼                ▼                ▼
    ┌───────────┐   ┌───────────┐   ┌───────────┐
    │   MySQL   │   │   Redis   │   │   MinIO   │
    └───────────┘   └───────────┘   └───────────┘
```

---

## 七、技术约束

### 7.1 版本要求

| 组件 | 最低版本 |
|------|----------|
| JDK | 17+ |
| Node.js | 18+ |
| MySQL | 8.0 |
| Maven | 3.9+ |

### 7.2 代码规范

- 命名规范：驼峰命名法
- RESTful API规范
- 分层架构：Controller → Service → Mapper
